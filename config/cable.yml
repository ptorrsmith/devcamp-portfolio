development:
  adapter: async

test:
  adapter: async

production:
  adapter: redis
  # url: <%= ENV.fetch("REDIS_URL") { "redis://www.ptorrsmith.com:6379/1" } %>
  url: <%= ENV["REDISCLOUD_URL"] %>
  # channel_prefix: DevcampPortfolio_production
